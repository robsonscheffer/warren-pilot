import { Fragment } from 'react'
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'

import { Grid } from '../../core/Storybook/Fragments.js'

import Message, { Received } from './index'

<Meta title="Molecules|Message" component={Message} />

### Message

label be USED on a `<TalkingBoard/>` it shows the text message and the origin "T"

### Message received

<Preview>
  <Story name="Text message received">
    <Grid columns={1}>
      <Received
        type="received"
        text="It's a dangerous business, Frodo, going out your door. ^1000 You step onto the road, and if you don't keep your feet, there's no knowing where you might be swept off to"
        label="T"
      />
    </Grid>
  </Story>
</Preview>

### Message sent

<Preview>
  <Story name="Text message sent">
    <Grid columns={1}>
      <Message
        type="sent"
        text="It's the job that's never started as takes longest to finish"
        label="T"
      />
    </Grid>
  </Story>
</Preview>

## Props

<Props of={Message} />
