import { Fragment } from 'react'
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'
import { Grid, Row } from '../../../../core/Storybook/Fragments'
import { Button } from '../../../../Atoms'
import { Input, Form } from '../../../../Atoms/Forms'

<Meta title="Atoms|Forms" component={Form} />

### Form

Open the console to watch the data submited

<Preview>
  <Story name="Form">
    <Grid columns={1}>
      <Form onSubmit={(data, el) => console.table(data, el)}>
        <Input type="text" placeholder="you@gmail.com" name="email" />
        <Button type="submit" size="small">
          Send Button
        </Button>
      </Form>
    </Grid>
  </Story>
</Preview>

## Props

Abstract component to form, we use here the `react-hook-form`

More on https://react-hook-form.com/get-started#Quickstart

<Props of={Form} />
